# ============================================================
# KizuLog ローカル開発環境設定
# ============================================================

# DB接続設定
spring.datasource.url=jdbc:postgresql://localhost:5432/kizulog
spring.datasource.username=kizulog
spring.datasource.password=${DB_PASSWORD:kizulog_dev}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA設定
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# ログ設定
logging.level.org.hibernate.SQL=DEBUG
logging.level.io.github.kizulogcommunity=DEBUG

# 暗号化方式(AES-256-GCM)
kizulog.crypto.secret-key=${CRYPTO_SECRET_KEY:dev-secret-key-32chars-minimum!}